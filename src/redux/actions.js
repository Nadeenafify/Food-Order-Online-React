export const add_item = (item) => ({
    type: 'ADD_ITEM',
    payload: item, 
  });
  export const remove_item = (item) => ({
    type: 'REMOVE_ITEM',
    payload: item, 
  });
  export const clear_cart = (item) => ({
    type: 'CLEAR_CART',
    payload: item, 
  });
  
  
  
 
  